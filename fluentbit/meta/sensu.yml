# Sample check
check:
  local_fluentbit_proc:
    command: "PATH=$PATH:/usr/lib64/nagios/plugins:/usr/lib/nagios/plugins check_procs -C fluentbit -u fluentbit -c 1:1"
    interval: 60
    occurrences: 1
    subscribers:
    - local-fluentbit-fluentbit
